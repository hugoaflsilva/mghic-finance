const Savings = {
    async load() {},
    showAddGoal() { App.openModal('modalSavingsGoal'); },
    saveGoal(event) { event.preventDefault(); },
    setIcon(icon, btn) {},
    setColor(color, btn) {},
    async loadGoalOptions() {}
};