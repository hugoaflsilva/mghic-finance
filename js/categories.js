const Categories = {
    async load() {},
    filter(type, btn) {},
    showAdd() { App.openModal('modalCategory'); },
    save(event) { event.preventDefault(); },
    setType(type, btn) {},
    setIcon(icon, btn) {},
    setColor(color, btn) {}
};